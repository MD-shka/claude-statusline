---
description: Подключить статусную строку Mocha Powerline
allowed-tools: Bash(sh:*)
---

Результат подключения:

!`sh "${CLAUDE_PLUGIN_ROOT}/hooks/wire-statusline.sh" --verbose`

Перескажи пользователю результат одной-двумя фразами. Если строка подключилась — добавь, что перезапускать Claude Code не нужно, она пересобирается на каждом кадре. Если сообщение говорит про чужую statusline или отсутствие `jq` — объясни, что делать дальше.
